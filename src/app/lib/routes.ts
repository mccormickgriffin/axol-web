const routes = {
  login: '/login',
  dashboard: '/dashboard',
  callback: '/callback',
};

const paths = {
  login: 'login',
  dashboard: 'dashboard',
  callback: 'callback',
};

export { routes, paths };
